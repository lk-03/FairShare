# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Mandatory Documentation Rule
Whenever you make ANY changes to the codebase (adding, editing, deleting, or renaming files, or changing architecture/state), you MUST update `ai-context.md` to keep the project guide and file directory accurate.

